interface ICategory {
  iconLink?: string,
  textLink: string
}

interface ICompArg {
  sectionClass: string, 
  blockClass: string, 
  pagination: string, 
  swiperBullets: string
}

export type { ICategory, ICompArg }